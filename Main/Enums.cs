namespace Main
{
    internal class Enums
    {
        public enum CrudChoice
        {
            ViewAll,
            Insert,
            Exit,
        }

        public enum MenuChoice
        {
            ManageHabits,
            ManageCategories,
            Exit,
        }
    }
}
